import React from 'react';
import PropTypes from 'prop-types';

const Admin = props => {
    return (
        <div>
            Welcome Admin
        </div>
    );
};

Admin.propTypes = {
    
};

export default Admin;